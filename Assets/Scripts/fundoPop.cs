using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class fundoPop : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 11.0f);
	}

	// Update is called once per frame
	void Update () {
		
	}
}
